<?php

return [
    // Database
    'db_host' => '127.0.0.1',
    'db_name' => 'lumosphere',
    'db_user' => 'lumosphere',
    'db_pass' => 'secret',

    // App
    'timezone'       => 'Europe/Paris',
    'allowed_origin' => 'https://your-domain.example.com',
    'setup_secret'   => 'change-me-before-first-run',

    // Sessions PHP — dossier privé hors /tmp partagé (évite la purge système à ~24 min sur o2switch).
    // Créer manuellement : mkdir -p /home2/mist2786/lumosphere/sessions && chmod 700 /home2/mist2786/lumosphere/sessions
    // Laisser vide ('') pour utiliser le save_path par défaut du serveur (déconseillé en production).
    'session_save_path' => '/home2/mist2786/lumosphere/sessions',

    // LiteLLM — legacy proxy mode (optional, overrides per-provider keys)
    'litellm_base_url' => '',
    'litellm_api_key'  => '',
    'litellm_model'    => '',

    // AI Provider API keys (one per provider, leave empty if unused)
    'mistral_api_key'   => '',
    'anthropic_api_key' => '',
    'openai_api_key'    => '',
    'gemini_api_key'    => '',
    'deepseek_api_key'  => '',

    // Telegram bot (cron collection)
    'telegram_bot_token' => '',

    // Workers (Python venv)
    'python_bin' => '/home2/mist2786/lumosphere/venvs/py311/bin/python3',
];
