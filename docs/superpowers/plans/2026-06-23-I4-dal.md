# Phase I.4 — Complétion de la DAL

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Compléter la couche d'accès aux données existante (corrections, point d'entrée API, couche de services front, tests).

**Architecture:** La DAL existe déjà dans `api/dal/` (11 fichiers PHP). On corrige 2 failles mineures, on ajoute un routeur API (`api/bootstrap.php`), une couche de services front (`src/services/api.ts`), et des tests PHPUnit. Aucune réécriture.

**Tech Stack:** PHP 8.4, PDO MySQL, PHPUnit, TypeScript, React 19.

## Global Constraints

- **PDO avec paramètres liés uniquement** — jamais de concaténation de chaînes dans le SQL.
- **Aucun SQL côté front** — tout passe par la couche de services → API → DAL.
- **Format de réponse standard** : `{status: 'ok'|'error', data, errors}`.
- **Conventions de nommage** : fonctions DAL = `dal_*`, fonctions atelier = `epuriel_*`, identifiants en anglais.
- **Ne pas modifier la logique métier existante** dans la DAL — seulement corriger les requêtes non préparées.
- **Cookies de session** : `httpOnly`, `Secure`, `SameSite=Lax`.
- **CSRF** : jeton dans `$_SESSION['csrf_token']`, vérifié via header `X-CSRF-Token` sur POST/PUT/DELETE.
- **CORS** : domaine exact (pas `*`), `credentials: true`.
- **Tests sur base de test dédiée**, pas la base de production.

---

### Task 1: Corriger les requêtes non préparées dans etats.php et roles.php

**Files:**
- Modify: `api/dal/etats.php:10-11`
- Modify: `api/dal/roles.php:10-11`

**Produces:**
- Toutes les requêtes DAL utilisent `prepare()`/`execute()` sans exception.

- [ ] **Step 1: Corriger `etats.php` — remplacer `$pdo->query()` par `prepare()`/`execute()`**

Dans `api/dal/etats.php`, fonction `dal_find_etats()`, remplacer :

```php
$rows = $pdo->query('SELECT id, nom, code, couleur, est_modifiable FROM etats ORDER BY id')
            ->fetchAll();
```

par :

```php
$stmt = $pdo->prepare('SELECT id, nom, code, couleur, est_modifiable FROM etats ORDER BY id');
$stmt->execute();
$rows = $stmt->fetchAll();
```

- [ ] **Step 2: Corriger `roles.php` — remplacer `$pdo->query()` par `prepare()`/`execute()`**

Dans `api/dal/roles.php`, fonction `dal_find_roles()`, remplacer :

```php
$rows = $pdo->query('SELECT id, nom, created_at, updated_at FROM roles ORDER BY id')
            ->fetchAll();
```

par :

```php
$stmt = $pdo->prepare('SELECT id, nom, created_at, updated_at FROM roles ORDER BY id');
$stmt->execute();
$rows = $stmt->fetchAll();
```

- [ ] **Step 3: Vérifier la syntaxe PHP**

Run: `php -l api/dal/etats.php && php -l api/dal/roles.php`
Expected: `No syntax errors detected` pour les deux fichiers.

- [ ] **Step 4: Vérifier PHPStan**

Run: `./vendor/bin/phpstan analyse api/dal/etats.php api/dal/roles.php`
Expected: aucune nouvelle erreur.

- [ ] **Step 5: Commit**

```bash
git add api/dal/etats.php api/dal/roles.php
git commit -m "fix(I.4): convertir les requêtes non préparées en prepare/execute"
```

---

### Task 2: Créer le point d'entrée API (bootstrap.php + routeur)

**Files:**
- Create: `api/bootstrap.php`
- Create: `api/router.php`
- Create: `api/endpoints/citations.php`
- Create: `api/endpoints/auteurs.php`
- Create: `api/endpoints/oeuvres.php`
- Create: `api/endpoints/themes.php`
- Create: `api/endpoints/keywords.php`
- Create: `api/endpoints/etats.php`
- Create: `api/endpoints/roles.php`
- Create: `api/endpoints/users.php`
- Create: `api/endpoints/config.php`
- Create: `api/endpoints/favorites.php`
- Create: `api/endpoints/auth.php`
- Create: `public/.htaccess`

**Consumes:** DAL corrigée (Task 1), `dal_get_pdo()` de `api/dal/core.php`.
**Produces:**
- `api/bootstrap.php` : charge la config, démarre la session, initialise PDO, configure CORS, vérifie CSRF, appelle le routeur.
- `api/router.php` : parse l'URL, dispatche vers le bon endpoint.
- `api/endpoints/*.php` : un fichier par ressource, traduit la requête HTTP en appel DAL.
- `public/.htaccess` : redirige `/api/*` vers `api/bootstrap.php`, et tout le reste vers `index.html` (SPA).

- [ ] **Step 1: Créer `api/bootstrap.php`**

```php
<?php

declare(strict_types=1);

// Session config
ini_set('session.cookie_httponly', '1');
ini_set('session.cookie_secure', '1');
ini_set('session.cookie_samesite', 'Lax');
session_start();

// Load config (outside repo)
$config = require dirname(__DIR__) . '/config/config.php';
date_default_timezone_set($config['timezone'] ?? 'Europe/Paris');

// PDO
require_once __DIR__ . '/dal/core.php';
$pdo = dal_get_pdo($config);

// CORS
$allowed_origin = $config['allowed_origin'] ?? '';
if ($allowed_origin !== '' && isset($_SERVER['HTTP_ORIGIN'])) {
    if ($_SERVER['HTTP_ORIGIN'] === $allowed_origin) {
        header("Access-Control-Allow-Origin: {$allowed_origin}");
        header('Access-Control-Allow-Credentials: true');
        header('Access-Control-Allow-Headers: Content-Type, X-CSRF-Token');
        header('Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS');
    }
}

// Preflight
if ($_SERVER['REQUEST_METHOD'] === 'OPTIONS') {
    http_response_code(204);
    exit;
}

// CSRF check on mutations
if (in_array($_SERVER['REQUEST_METHOD'], ['POST', 'PUT', 'DELETE'], true)) {
    $token = $_SERVER['HTTP_X_CSRF_TOKEN'] ?? '';
    $session_token = $_SESSION['csrf_token'] ?? '';
    // Skip CSRF for login endpoint
    $uri = parse_url($_SERVER['REQUEST_URI'], PHP_URL_PATH);
    if (!str_ends_with($uri, '/auth/login') && ($token === '' || $token !== $session_token)) {
        http_response_code(403);
        echo json_encode(['status' => 'error', 'data' => null, 'errors' => ['Jeton CSRF invalide.']]);
        exit;
    }
}

// Build user context
$ctx = [
    'user_id'         => $_SESSION['user_id'] ?? null,
    'role_id'         => $_SESSION['role_id'] ?? ROLE_VISITEUR,
    'permissions'     => $_SESSION['permissions'] ?? ['corpus.read'],
    'include_deleted' => false,
];

// JSON output
header('Content-Type: application/json; charset=utf-8');

// Route
require_once __DIR__ . '/router.php';
route($pdo, $ctx);
```

- [ ] **Step 2: Créer `api/router.php`**

```php
<?php

declare(strict_types=1);

function route(PDO $pdo, array $ctx): void
{
    $method = $_SERVER['REQUEST_METHOD'];
    $uri = parse_url($_SERVER['REQUEST_URI'], PHP_URL_PATH);

    // Strip /api/ prefix
    $path = preg_replace('#^/api/#', '', $uri);
    $segments = explode('/', trim($path, '/'));
    $resource = $segments[0] ?? '';
    $id = isset($segments[1]) && ctype_digit($segments[1]) ? (int) $segments[1] : null;
    $action = $segments[2] ?? null;

    $endpoint_file = __DIR__ . '/endpoints/' . $resource . '.php';
    if ($resource === '' || !file_exists($endpoint_file)) {
        http_response_code(404);
        echo json_encode(dal_error('Ressource introuvable.'));
        return;
    }

    require_once $endpoint_file;
    $handler = "endpoint_{$resource}";
    if (!function_exists($handler)) {
        http_response_code(404);
        echo json_encode(dal_error('Ressource introuvable.'));
        return;
    }

    $body = null;
    if (in_array($method, ['POST', 'PUT'], true)) {
        $body = json_decode(file_get_contents('php://input'), true) ?? [];
    }

    try {
        $result = $handler($pdo, $ctx, $method, $id, $body, $action);
        echo json_encode($result);
    } catch (RuntimeException $e) {
        http_response_code(403);
        echo json_encode(dal_error($e->getMessage()));
    } catch (\Throwable $e) {
        http_response_code(500);
        echo json_encode(dal_error('Erreur interne du serveur.'));
    }
}
```

- [ ] **Step 3: Créer un endpoint type — `api/endpoints/citations.php`**

```php
<?php

declare(strict_types=1);

require_once dirname(__DIR__) . '/dal/citations.php';

function endpoint_citations(PDO $pdo, array $ctx, string $method, ?int $id, ?array $body, ?string $action): array
{
    return match (true) {
        $method === 'GET' && $id === null && isset($_GET['q']) => dal_search_citations(
            $pdo, $ctx, $_GET['q'],
            array_filter([
                'oeuvre_id'    => $_GET['oeuvre_id'] ?? null,
                'theme_id'    => $_GET['theme_id'] ?? null,
                'etat_id'     => $_GET['etat_id'] ?? null,
                'auteur_id'   => $_GET['auteur_id'] ?? null,
                'keyword_ids' => isset($_GET['keyword_ids']) ? explode(',', $_GET['keyword_ids']) : null,
                'keyword_mode'=> $_GET['keyword_mode'] ?? null,
                'date_from'   => $_GET['date_from'] ?? null,
                'date_to'     => $_GET['date_to'] ?? null,
            ]),
            $_GET['cursor'] ?? null,
            (int) ($_GET['page_size'] ?? PAGE_SIZE_DEFAULT)
        ),
        $method === 'GET' && $id === null => dal_find_citations(
            $pdo, $ctx,
            array_filter([
                'oeuvre_id'    => $_GET['oeuvre_id'] ?? null,
                'theme_id'    => $_GET['theme_id'] ?? null,
                'etat_id'     => $_GET['etat_id'] ?? null,
                'auteur_id'   => $_GET['auteur_id'] ?? null,
                'keyword_ids' => isset($_GET['keyword_ids']) ? explode(',', $_GET['keyword_ids']) : null,
                'keyword_mode'=> $_GET['keyword_mode'] ?? null,
                'date_from'   => $_GET['date_from'] ?? null,
                'date_to'     => $_GET['date_to'] ?? null,
            ]),
            $_GET['cursor'] ?? null,
            (int) ($_GET['page_size'] ?? PAGE_SIZE_DEFAULT)
        ),
        $method === 'GET' && $id !== null => dal_get_citation($pdo, $ctx, $id),
        $method === 'POST' && $id === null => dal_create_citation($pdo, $ctx, $body ?? []),
        $method === 'PUT' && $id !== null && $action === 'keywords' => dal_set_citation_keywords(
            $pdo, $ctx, $id, $body['keyword_ids'] ?? []
        ),
        $method === 'PUT' && $id !== null => dal_update_citation($pdo, $ctx, $id, $body ?? []),
        $method === 'DELETE' && $id !== null => dal_soft_delete_citation($pdo, $ctx, $id),
        default => dal_error('Méthode non supportée.'),
    };
}
```

- [ ] **Step 4: Créer les endpoints restants**

Créer un fichier par ressource dans `api/endpoints/` suivant le même modèle que citations.php. Chaque fichier :
- Inclut le fichier DAL correspondant.
- Définit `endpoint_{resource}()` avec le même signature.
- Mappe GET/POST/PUT/DELETE vers les fonctions `dal_*` correspondantes.

Fichiers à créer : `auteurs.php`, `oeuvres.php`, `themes.php`, `keywords.php`, `etats.php`, `roles.php`, `users.php`, `config.php`, `favorites.php`, `auth.php`.

L'endpoint `auth.php` est un squelette minimal (login/logout/csrf) — l'implémentation complète viendra en T07 (migration §6.2).

```php
<?php
// api/endpoints/auth.php

declare(strict_types=1);

require_once dirname(__DIR__) . '/dal/users.php';

function endpoint_auth(PDO $pdo, array $ctx, string $method, ?int $id, ?array $body, ?string $action): array
{
    return match (true) {
        $method === 'GET' && $action === 'csrf' => _auth_csrf(),
        $method === 'GET' && $action === 'me' => _auth_me($ctx),
        default => dal_error('Méthode non supportée.'),
    };
}

function _auth_csrf(): array
{
    if (empty($_SESSION['csrf_token'])) {
        $_SESSION['csrf_token'] = bin2hex(random_bytes(32));
    }
    return dal_ok(['csrf_token' => $_SESSION['csrf_token']]);
}

function _auth_me(array $ctx): array
{
    if ($ctx['user_id'] === null) {
        return dal_ok(null);
    }
    return dal_ok([
        'user_id' => $ctx['user_id'],
        'role_id' => $ctx['role_id'],
    ]);
}
```

- [ ] **Step 5: Créer `public/.htaccess`**

```apache
RewriteEngine On

# API requests → bootstrap.php
RewriteCond %{REQUEST_URI} ^/api/
RewriteRule ^api/(.*)$ /api/bootstrap.php [L,QSA]

# SPA fallback (non-file, non-directory, non-api)
RewriteCond %{REQUEST_URI} !^/api/
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
```

- [ ] **Step 6: Vérifier la syntaxe PHP de tous les nouveaux fichiers**

Run: `php -l api/bootstrap.php && php -l api/router.php && for f in api/endpoints/*.php; do php -l "$f"; done`
Expected: `No syntax errors detected` pour chaque fichier.

- [ ] **Step 7: Commit**

```bash
git add api/bootstrap.php api/router.php api/endpoints/ public/.htaccess
git commit -m "feat(I.4): point d'entrée API avec routeur, CORS, CSRF, endpoints"
```

---

### Task 3: Créer la couche de services front (api.ts)

**Files:**
- Create: `src/services/api.ts`

**Consumes:** Endpoints API (Task 2) — tous les chemins `/api/{resource}`.
**Produces:**
- `apiClient` : objet unique centralisant tous les appels au serveur.
- Chaque méthode retourne `Promise<ApiResponse<T>>` avec `{status, data, errors}`.

- [ ] **Step 1: Créer `src/services/api.ts`**

```typescript
type ApiResponse<T> = {
  status: 'ok' | 'error'
  data: T | null
  errors: string[]
}

let csrfToken: string | null = null

async function request<T>(
  path: string,
  options: RequestInit = {},
): Promise<ApiResponse<T>> {
  const headers: Record<string, string> = {
    'Content-Type': 'application/json',
    ...(options.headers as Record<string, string>),
  }

  if (options.method && ['POST', 'PUT', 'DELETE'].includes(options.method)) {
    if (!csrfToken) {
      const csrf = await fetchCsrf()
      csrfToken = csrf.data?.csrf_token ?? null
    }
    if (csrfToken) {
      headers['X-CSRF-Token'] = csrfToken
    }
  }

  const response = await fetch(`/api/${path}`, {
    ...options,
    headers,
    credentials: 'include',
  })

  if (response.status === 403) {
    csrfToken = null
  }

  return response.json() as Promise<ApiResponse<T>>
}

function get<T>(path: string): Promise<ApiResponse<T>> {
  return request<T>(path)
}

function post<T>(path: string, body: unknown): Promise<ApiResponse<T>> {
  return request<T>(path, {
    method: 'POST',
    body: JSON.stringify(body),
  })
}

function put<T>(path: string, body: unknown): Promise<ApiResponse<T>> {
  return request<T>(path, {
    method: 'PUT',
    body: JSON.stringify(body),
  })
}

function del<T>(path: string): Promise<ApiResponse<T>> {
  return request<T>(path, { method: 'DELETE' })
}

function fetchCsrf() {
  return get<{ csrf_token: string }>('auth/csrf')
}

export const apiClient = {
  // Auth
  getCsrf: fetchCsrf,
  getMe: () => get<{ user_id: number; role_id: number } | null>('auth/me'),

  // Citations
  findCitations: (params?: Record<string, string>) => {
    const qs = params ? '?' + new URLSearchParams(params).toString() : ''
    return get<{ items: unknown[]; next_cursor: string | null }>(`citations${qs}`)
  },
  getCitation: (id: number) => get<unknown>(`citations/${id}`),
  createCitation: (data: unknown) => post<{ id: number }>('citations', data),
  updateCitation: (id: number, data: unknown) => put<{ id: number }>(`citations/${id}`, data),
  deleteCitation: (id: number) => del<void>(`citations/${id}`),
  setCitationKeywords: (id: number, keywordIds: number[]) =>
    put<void>(`citations/${id}/keywords`, { keyword_ids: keywordIds }),

  // Auteurs
  findAuteurs: (params?: Record<string, string>) => {
    const qs = params ? '?' + new URLSearchParams(params).toString() : ''
    return get<unknown[]>(`auteurs${qs}`)
  },
  getAuteur: (id: number) => get<unknown>(`auteurs/${id}`),
  createAuteur: (data: unknown) => post<{ id: number }>('auteurs', data),
  updateAuteur: (id: number, data: unknown) => put<{ id: number }>(`auteurs/${id}`, data),
  deleteAuteur: (id: number) => del<void>(`auteurs/${id}`),

  // Oeuvres
  findOeuvres: (params?: Record<string, string>) => {
    const qs = params ? '?' + new URLSearchParams(params).toString() : ''
    return get<unknown[]>(`oeuvres${qs}`)
  },
  getOeuvre: (id: number) => get<unknown>(`oeuvres/${id}`),
  createOeuvre: (data: unknown) => post<{ id: number }>('oeuvres', data),
  updateOeuvre: (id: number, data: unknown) => put<{ id: number }>(`oeuvres/${id}`, data),
  deleteOeuvre: (id: number) => del<void>(`oeuvres/${id}`),

  // Themes
  findThemes: () => get<unknown[]>('themes'),
  getTheme: (id: number) => get<unknown>(`themes/${id}`),
  createTheme: (data: unknown) => post<{ id: number }>('themes', data),
  updateTheme: (id: number, data: unknown) => put<{ id: number }>(`themes/${id}`, data),
  deleteTheme: (id: number) => del<void>(`themes/${id}`),

  // Keywords
  findKeywords: (params?: Record<string, string>) => {
    const qs = params ? '?' + new URLSearchParams(params).toString() : ''
    return get<unknown[]>(`keywords${qs}`)
  },
  createKeyword: (data: unknown) => post<{ id: number }>('keywords', data),
  deleteKeyword: (id: number) => del<void>(`keywords/${id}`),

  // Etats
  findEtats: () => get<unknown[]>('etats'),

  // Roles
  findRoles: () => get<unknown[]>('roles'),
  getRoleWithPermissions: (id: number) => get<unknown>(`roles/${id}`),
  updateRolePermissions: (id: number, permissionIds: number[]) =>
    put<void>(`roles/${id}/permissions`, { permission_ids: permissionIds }),

  // Users
  findUsers: () => get<unknown[]>('users'),
  getUser: (id: number) => get<unknown>(`users/${id}`),
  createUser: (data: unknown) => post<{ id: number }>('users', data),
  updateUser: (id: number, data: unknown) => put<{ id: number }>(`users/${id}`, data),
  deleteUser: (id: number) => del<void>(`users/${id}`),

  // Config
  getConfig: (key: string) => get<unknown>(`config/${key}`),
  setConfig: (key: string, value: string) => put<void>(`config/${key}`, { value }),

  // Favorites
  findFavorites: (params?: Record<string, string>) => {
    const qs = params ? '?' + new URLSearchParams(params).toString() : ''
    return get<{ items: unknown[]; next_cursor: string | null }>(`favorites${qs}`)
  },
  addFavorite: (citationId: number) => post<void>('favorites', { citation_id: citationId }),
  removeFavorite: (citationId: number) => del<void>(`favorites/${citationId}`),
}
```

- [ ] **Step 2: Vérifier le type-check**

Run: `pnpm tsc --noEmit`
Expected: 0 erreurs.

- [ ] **Step 3: Vérifier le lint**

Run: `pnpm lint`
Expected: 0 erreurs.

- [ ] **Step 4: Commit**

```bash
git add src/services/api.ts
git commit -m "feat(I.4): couche de services front (apiClient) — point unique d'appel API"
```

---

### Task 4: Tests PHPUnit de la DAL

**Files:**
- Create: `tests/dal/CitationsTest.php`
- Create: `tests/dal/RolesAccessTest.php`
- Create: `tests/dal/SearchTest.php`
- Create: `tests/dal/ConcurrencyTest.php`
- Create: `tests/dal/TestHelper.php`
- Modify: `phpunit.xml`

**Consumes:** Toutes les fonctions `dal_*` de `api/dal/`.
**Produces:** Suite de tests PHPUnit couvrant les règles métier, les droits, la recherche et la concurrence.

- [ ] **Step 1: Créer `tests/dal/TestHelper.php` — utilitaires de test**

```php
<?php

declare(strict_types=1);

namespace Tests\Dal;

require_once __DIR__ . '/../../api/dal/core.php';

class TestHelper
{
    public static function getTestPdo(): \PDO
    {
        $config = [
            'db_host' => getenv('TEST_DB_HOST') ?: '127.0.0.1',
            'db_name' => getenv('TEST_DB_NAME') ?: 'lumosphere_test',
            'db_user' => getenv('TEST_DB_USER') ?: 'root',
            'db_pass' => getenv('TEST_DB_PASS') ?: '',
        ];
        return dal_get_pdo($config);
    }

    public static function ctxAdmin(): array
    {
        return [
            'user_id' => 1,
            'role_id' => ROLE_ADMIN,
            'permissions' => ['corpus.read', 'corpus.write', 'corpus.delete', 'admin.settings', 'admin.roles', 'admin.users'],
            'include_deleted' => false,
        ];
    }

    public static function ctxEditeur(): array
    {
        return [
            'user_id' => 2,
            'role_id' => ROLE_EDITEUR,
            'permissions' => ['corpus.read', 'corpus.write', 'corpus.delete'],
            'include_deleted' => false,
        ];
    }

    public static function ctxVisiteur(): array
    {
        return [
            'user_id' => null,
            'role_id' => ROLE_VISITEUR,
            'permissions' => ['corpus.read'],
            'include_deleted' => false,
        ];
    }

    public static function ctxAbo3(int $userId = 10): array
    {
        return [
            'user_id' => $userId,
            'role_id' => ROLE_ABO3,
            'permissions' => ['corpus.read'],
            'include_deleted' => false,
        ];
    }
}
```

- [ ] **Step 2: Créer `tests/dal/CitationsTest.php` — règles métier**

```php
<?php

declare(strict_types=1);

namespace Tests\Dal;

use PHPUnit\Framework\TestCase;

require_once __DIR__ . '/../../api/dal/citations.php';
require_once __DIR__ . '/../../api/dal/etats.php';
require_once __DIR__ . '/TestHelper.php';

class CitationsTest extends TestCase
{
    private \PDO $pdo;

    protected function setUp(): void
    {
        $this->pdo = TestHelper::getTestPdo();
        $this->pdo->beginTransaction();
    }

    protected function tearDown(): void
    {
        $this->pdo->rollBack();
    }

    public function testCreateCitationDefaultsToAcorriger(): void
    {
        $ctx = TestHelper::ctxEditeur();
        $result = dal_create_citation($this->pdo, $ctx, [
            'contenu' => 'Test citation',
            'oeuvre_id' => 1,
        ]);
        $this->assertSame('ok', $result['status']);
        $id = $result['data']['id'];

        $citation = dal_get_citation($this->pdo, $ctx, $id);
        $this->assertSame(ETAT_A_CORRIGER, (int) $citation['data']['etat_id']);
    }

    public function testCannotPublishWithoutRequiredFields(): void
    {
        $ctx = TestHelper::ctxEditeur();
        $result = dal_create_citation($this->pdo, $ctx, [
            'contenu' => 'Test citation sans theme',
            'oeuvre_id' => 1,
        ]);
        $id = $result['data']['id'];

        $update = dal_update_citation($this->pdo, $ctx, $id, [
            'etat_id' => ETAT_PUBLIEE,
        ]);
        $this->assertSame('error', $update['status']);
        $this->assertStringContainsString('theme', $update['errors'][0]);
    }

    public function testSoftDeleteAndRestore(): void
    {
        $ctx = TestHelper::ctxEditeur();
        $adminCtx = TestHelper::ctxAdmin();
        $result = dal_create_citation($this->pdo, $ctx, [
            'contenu' => 'A supprimer',
            'oeuvre_id' => 1,
        ]);
        $id = $result['data']['id'];

        dal_soft_delete_citation($this->pdo, $ctx, $id);
        $get = dal_get_citation($this->pdo, $ctx, $id);
        $this->assertSame('error', $get['status']);

        $adminCtxDeleted = array_merge($adminCtx, ['include_deleted' => true]);
        $getAdmin = dal_get_citation($this->pdo, $adminCtxDeleted, $id);
        $this->assertSame('ok', $getAdmin['status']);

        dal_restore_citation($this->pdo, $adminCtx, $id);
        $getRestored = dal_get_citation($this->pdo, $ctx, $id);
        $this->assertSame('ok', $getRestored['status']);
    }

    public function testSystemStatesCannotBeDeleted(): void
    {
        $ctx = TestHelper::ctxAdmin();
        $result = dal_delete_etat($this->pdo, $ctx, ETAT_A_CORRIGER);
        $this->assertSame('error', $result['status']);
        $this->assertStringContainsString('système', $result['errors'][0]);
    }
}
```

- [ ] **Step 3: Créer `tests/dal/RolesAccessTest.php` — droits par rôle**

```php
<?php

declare(strict_types=1);

namespace Tests\Dal;

use PHPUnit\Framework\TestCase;

require_once __DIR__ . '/../../api/dal/citations.php';
require_once __DIR__ . '/TestHelper.php';

class RolesAccessTest extends TestCase
{
    private \PDO $pdo;

    protected function setUp(): void
    {
        $this->pdo = TestHelper::getTestPdo();
        $this->pdo->beginTransaction();
    }

    protected function tearDown(): void
    {
        $this->pdo->rollBack();
    }

    public function testVisiteurSeesOnlyPublishedCitations(): void
    {
        $ctx = TestHelper::ctxVisiteur();
        $result = dal_find_citations($this->pdo, $ctx);
        $this->assertSame('ok', $result['status']);
        foreach ($result['data']['items'] as $item) {
            $this->assertSame(ETAT_PUBLIEE, (int) $item['etat_id']);
        }
    }

    public function testEditeurSeesAllStates(): void
    {
        $ctx = TestHelper::ctxEditeur();
        $result = dal_find_citations($this->pdo, $ctx);
        $this->assertSame('ok', $result['status']);
    }

    public function testAdminRoleCannotBeDeleted(): void
    {
        require_once __DIR__ . '/../../api/dal/roles.php';
        $ctx = TestHelper::ctxAdmin();
        $result = dal_delete_role($this->pdo, $ctx, ROLE_ADMIN);
        $this->assertSame('error', $result['status']);
        $this->assertStringContainsString('Administrateur', $result['errors'][0]);
    }

    public function testVisiteurCannotWriteCitation(): void
    {
        $this->expectException(\RuntimeException::class);
        $ctx = TestHelper::ctxVisiteur();
        dal_create_citation($this->pdo, $ctx, [
            'contenu' => 'Should fail',
            'oeuvre_id' => 1,
        ]);
    }
}
```

- [ ] **Step 4: Créer `tests/dal/SearchTest.php` — recherche fulltext**

```php
<?php

declare(strict_types=1);

namespace Tests\Dal;

use PHPUnit\Framework\TestCase;

require_once __DIR__ . '/../../api/dal/citations.php';
require_once __DIR__ . '/TestHelper.php';

class SearchTest extends TestCase
{
    private \PDO $pdo;

    protected function setUp(): void
    {
        $this->pdo = TestHelper::getTestPdo();
        $this->pdo->beginTransaction();
    }

    protected function tearDown(): void
    {
        $this->pdo->rollBack();
    }

    public function testSearchReturnsStandardFormat(): void
    {
        $ctx = TestHelper::ctxEditeur();
        $result = dal_search_citations($this->pdo, $ctx, 'test');
        $this->assertSame('ok', $result['status']);
        $this->assertArrayHasKey('items', $result['data']);
        $this->assertArrayHasKey('next_cursor', $result['data']);
    }

    public function testEmptySearchFallsBackToFind(): void
    {
        $ctx = TestHelper::ctxEditeur();
        $result = dal_search_citations($this->pdo, $ctx, '');
        $this->assertSame('ok', $result['status']);
    }

    public function testKeywordFilterOrMode(): void
    {
        $ctx = TestHelper::ctxEditeur();
        $result = dal_find_citations($this->pdo, $ctx, [
            'keyword_ids' => [1, 2],
            'keyword_mode' => 'or',
        ]);
        $this->assertSame('ok', $result['status']);
    }

    public function testKeywordFilterAndMode(): void
    {
        $ctx = TestHelper::ctxEditeur();
        $result = dal_find_citations($this->pdo, $ctx, [
            'keyword_ids' => [1, 2],
            'keyword_mode' => 'and',
        ]);
        $this->assertSame('ok', $result['status']);
    }
}
```

- [ ] **Step 5: Mettre à jour `phpunit.xml`**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true">
    <testsuites>
        <testsuite name="dal">
            <directory>tests/dal</directory>
        </testsuite>
    </testsuites>
</phpunit>
```

- [ ] **Step 6: Vérifier la syntaxe PHP**

Run: `for f in tests/dal/*.php; do php -l "$f"; done`
Expected: `No syntax errors detected` pour chaque fichier.

- [ ] **Step 7: Lancer les tests (nécessite une base de test)**

Run: `./vendor/bin/phpunit --testsuite dal`
Expected: les tests passent si une base `lumosphere_test` est configurée. Sinon, documenter la configuration requise.

Note : les tests de concurrence (`ConcurrencyTest.php`) et les tests d'accent-insensibilité de la recherche fulltext nécessitent une base MySQL avec les données de test (Phase I.7, T10). Ils seront validés à ce moment-là. Les tests de structure et de logique passent dès maintenant.

- [ ] **Step 8: Commit**

```bash
git add tests/dal/ phpunit.xml
git commit -m "test(I.4): tests PHPUnit pour la DAL — règles métier, droits, recherche"
```

---

### Task 5: Mise à jour de la documentation

**Files:**
- Modify: `docs/3-devbook_developpement-lumosphere.md`
- Modify: `docs/1-trame_execution-lumosphere.md`
- Modify: `docs/note_composants-I2.md`

**Produces:** Documentation à jour reflétant l'état réel du projet.

- [ ] **Step 1: Mettre à jour la note composants — ajouter les nouveaux fichiers créés**

Dans `docs/note_composants-I2.md`, ajouter dans la section « Présent dans le dépôt (local) » :

| Élément | Version | Emplacement |
|---|---|---|
| Point d'entrée API | — | `api/bootstrap.php` + `api/router.php` |
| Endpoints API | — | `api/endpoints/` (11 fichiers) |
| Services front | — | `src/services/api.ts` |
| Tests DAL | — | `tests/dal/` |

- [ ] **Step 2: Commit**

```bash
git add docs/
git commit -m "docs(I.4): mise à jour documentation après complétion DAL"
```

Note : les cases I.4 du devbook ne seront cochées qu'après validation du chef de projet.

---

## Vérification post-plan

| Commande | Résultat attendu |
|---|---|
| `php -l api/bootstrap.php` | Pas d'erreur de syntaxe |
| `php -l api/router.php` | Pas d'erreur de syntaxe |
| `for f in api/endpoints/*.php; do php -l "$f"; done` | Pas d'erreur de syntaxe |
| `./vendor/bin/phpstan analyse` | Aucune erreur |
| `pnpm tsc --noEmit` | 0 erreur TypeScript |
| `pnpm lint` | 0 erreur |
| `pnpm build` | Build réussi |
| `./vendor/bin/phpunit --testsuite dal` | Tests passent |
| `gitleaks detect -v` | Aucun secret |
